jobs:
  test
  executor: node/default
    steps:
      - checkout
      - node/install-packages:
          cache-path: ~/esn-frontend-calendar/node_modules
          override-ci-command: npm install
      - run: npm run build
orbs:
  node: 'circleci/node:10'
version: 2.1
workflows:
  test_my_app:
    jobs:
      - test
